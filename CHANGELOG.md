# 更新日志 · 精卫 Jingwei

本文件遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的写法，版本号遵循语义化版本。

---

## [1.0.0] — 2026-09-22

精卫正式作为「大云壁画工具箱」的一员发布。本次是**整理发布**，图像算法没有做任何改动。

### 新增

- 统一的系列页头：工具箱名称、工具名、版本号、服务状态、端口。
- `GET /api/health` 健康检查接口，供启动脚本与工具箱启动台探测。
- 公共底座 `toolkit_core.py`：安全路径解析、受限 CORS、分块传输、统一错误结构、统一启动横幅。
- 品牌资源 `assets/logo.svg`（圆形朱文印 · 双翼衔石）。
- 统一启动脚本 `run.bat` / `run.ps1`：自动定位 Python、检查依赖、健康检查、自动打开浏览器。
- `.gitignore`：运行产物一律不进仓库。

### 变更

- **界面重做**：前端拆分为 `web/index.html` + `web/styles.css` + `web/app.js`，采用全系列统一的设计 token（白底、细边线、近黑主按钮）。
- **导出目录**：默认输出从仓库外的 `..\PatchAligner-Output` 改为仓库内的 `exports/`，不再在工作区根目录留下散落目录。
- **错误提示**：后端统一返回 `{"error": {"code", "message", "field", "detail"}}`，前端直接显示中文 `message`，技术细节放在 `detail`。
- **文件名处理**：上传文件名统一走底座的 `safe_filename`，保留中文、过滤非法字符。
- 版本来源统一为 `server.py` 的 `APP_VERSION`，页面与启动脚本不再各自写死。

### 修复

- **路径越界（安全问题）**：旧版静态文件回退使用字符串前缀判断，形同虚设；现在所有静态与输出文件都必须经过 `toolkit_core.safe_join()` 的真路径包含校验，越界一律 403。
- **CORS 缺失**：补上 `OPTIONS` 预检，仅放行 `http://127.0.0.1:*`、`http://localhost:*` 和 `Origin: null`，其它来源不发放 CORS 头。
- **整文件读入内存**：静态文件与结果文件改为 1 MB 分块传输，不再 `read_bytes()` 一次性读入。
- **参数无上限**：搜索长边、检测长边、羽化像素、导出长边、裁剪框都加了范围校验，越界返回带中文说明的 400，而不是让服务被拖死。
- **上传无上限**：`/api/analyze`、`/api/upload_pair`、`/api/crop_pair` 都声明了体积上限并返回明确提示。
- **端口硬编码**：旧启动脚本写死了某台机器上的 Python 绝对路径，换机器即失效；现在由脚本自动查找。

### 移除

- 前端一段重复的 `wireDrop` 实现（两个 script 块各写了一遍），合并为一份。
- 页面上写死的 "v0.4" 版本字符串。

### 保留

- 全部图像算法逐行不变：SIFT 特征匹配、CLAHE 预处理、FLANN + RATIO TEST、RANSAC 相似变换、旋转小于 6° 时的最小二乘「平移 + 等比缩放」回退、羽化遮罩、并排对比图生成、STORE 模式打包。

### 已知待办

1. `parse_multipart_streaming()` 目前**尚未接入**。两个上传接口仍是「先读完整段请求体再解析」，意味着一次 1 GB 的上传会占用约 1 GB 内存。该函数已写好但从未做过回归验证，贸然启用有风险，因此本次保留在文件里并标注「未启用」。接入前需要补上传体积、断流、损坏 multipart 的测试。
2. 区域裁剪的会话目录 `pair_sessions/` 目前不会自动清理，长期使用会占磁盘。建议后续加「启动时清理超过 N 天的会话」。
3. 历史记录 `runs/` 同样只增不减，建议在界面上提供「清理旧记录」。

---

## 整理之前的版本

以下为整理前的历史（目录名与版本号当时并不一致，这里按源码里的 `APP_VERSION` 记录）：

| 源码标注 | 说明 |
|---|---|
| `0.4` | 前端页头显示 `v0.4`，含「回贴定位 / 对比导出 / 区域裁剪」三条工作流 |
| `0.2` | `server_version = "LocalPatchAligner/0.2"`，更早的版本 |

整理前的目录名为 `local-patch-aligner`，现更名为 `jingwei`。
